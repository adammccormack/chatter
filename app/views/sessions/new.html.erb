<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
  </head>
  <body>

  <% if current_user %>
    <h1>Welcome, <%= current_user.email %></h1>
  <% else %>
    Please check your email or password
  <% end %>

  <section class="section">

    <h1 class="title">Login</h1>

    <%= form_with(scope: :session, url: '/login') do |f| %>
      <div class="field">
        <p><%= f.label :email, class: 'label' %></p>
        <div class="control">
          <p><%= f.email_field :email, class: 'input' %></p>
        </div>
      </div>
      <div class="field">
       <p><%= f.label :password, class: 'label' %></p>
       <div class="control">
         <p><%= f.password_field :password, class: 'input' %></p>
       </div>
      </div>
       <%= f.submit "Sign in", class: 'button is-primary' %>
      </div>
    <% end %>
      
  </section>

  </body>
</html>

